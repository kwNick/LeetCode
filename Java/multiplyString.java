class multiplyString {
    public String multiply(String num1, String num2) {
        return Integer.toString(Integer.parseInt(num1) * Integer.parseInt(num2));
    }
}

//parses to number multiplies them and parses back to string